# Core Python version requirement
# python_version >= "3.10"
numpy
# FAISS CPU version
faiss-cpu

# For local vector store & retrieval
langchain_community
langchain_core
langchain_text_splitters
langchain_huggingface
langchain_ollama

# PDF parsing
pdfplumber

# DOCX parsing
python-docx

# XLSX parsing
openpyxl

# System memory checks
psutil

# YAML config
PyYAML

# Gradio UI
gradio

# Web search and scraping
duckduckgo-search
requests
beautifulsoup4

# Regex, logging, os, re are built-in, no need to specify

transformers
sentence-transformers
torch
huggingface_hub

# If you run into missing dependencies, consider also installing:
# accelerate
# bitsandbytes (for quantized models, if used)

# For OCR :
pytesseract>=0.3.10
Pillow>=9.5.0